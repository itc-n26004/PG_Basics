def squared(x):
    """
    渡された数値を2乗した結果を返す。

    引数：
    x ----- 数値（整数または浮動小数点数）
    戻り値：
    xを2乗した結果
    """
    return x ** 2

print(squared(4))


def print_sakana(sakana):
    """
    渡された値をそのまま画面に出力（表示）する。

    引数：
    sakana ----- 出力したい文字列や数値など
    """
    print(sakana)

print_sakana("Testing: 1,2,3.")


def sakana(a, b, c, x=4, z=5):
    """
    3つの必須引数と2つの任意引数、計5つの数値をすべて足し算した結果を返す。

    引数：
    a ----- 必須引数（数値）
    b ----- 必須引数（数値）
    c ----- 必須引数（数値）
    x ----- 任意引数（初期値: 4）
    z ----- 任意引数（初期値: 5）
    戻り値：
    すべての引数を合計した値
    """
    return a + b + c + x + z

result = sakana(1,2,3)
print(result)


def sakana(x):
    """
    入力された整数を2で割った商（切り捨て）を返す。
    処理の途中で固定の計算結果メッセージを出力する。

    引数：
    x ----- 必須引数（整数）
    戻り値：
    xを2で割った商
    """
    print("入力された整数を2で割った結果=", 3)
    return  x // 2


def ction(x):
    """
    入力された数値に4を掛けた結果を返す。
    処理の途中で固定の計算結果メッセージを出力する。

    引数：
    x ----- 必須引数（数値）
    戻り値：
    xに4を掛けた値
    """
    print("入力された整数を4でかけた結果=", 12 )
    return  x * 4

y=sakana(6)
z=ction(y)


def decimal(a):
    """
    この関数は、文字列を浮動小数点数に変換する。
    その結果を出力してプログラムに返す。
    例外処理として、ValueError の時にメッセージを出力してプログラムに返す。
    
    引数：
    a ----- 必須引数
    戻り値：
    result ---- 引数a（※元の処理通りaを返します）
    """
    try:
        print("入力された文字 ＝", a)
        print("入力された文字を少数点化した結果＝", float(a))
        return a
    except ValueError:
        print("整数、または、少数点数を入力してください。")

#a = str(4)                                                                                            #a = str("あ")
a = str("4")
decimal(a)
